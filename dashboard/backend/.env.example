GCS_BUCKET=your-gcs-bucket
GCS_PREFIX=gold/
GCP_PROJECT=your-gcp-project
DUCKDB_PATH=
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# Optional overrides. Defaults point to app/utils/maps inside the backend image.
NGHIA_DO_NET_XML_PATH=
NGHIA_DO_OSM_XML_PATH=
